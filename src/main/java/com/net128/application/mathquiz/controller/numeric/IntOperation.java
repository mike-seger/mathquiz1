package com.net128.application.mathquiz.controller.numeric;
public interface IntOperation {
	int operate(int op1, int op2);
	int invOperate(int op1, int op2);
	int maxVariations(double low, double high);
}
